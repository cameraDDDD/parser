#ifndef PARSER_H
#define PARSER_H

/* Replace all occurrences of OLD_STR with NEW_STR from src into dest.
    dest must be large enough to hold the result. */
void replace_name(const char *src, char *dest);

#endif
